let a: number = 3;
let b: number = 2;
let c: number = 6;

function multiplicar(a:number, b:number, c:number): void {
    console.info("O resultado é = "
    + a * b * c + " ");
}

multiplicar(a, b, c);

let y = "Olá ";
let x = "Ana";

function concatenar(y: string, x: string): any {
    console.info
    ("saudação _ " + y + x );
}

concatenar(y, x);